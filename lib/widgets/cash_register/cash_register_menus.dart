import 'package:flutter/material.dart';
import 'package:order2eat/widgets/menus/menus_future.dart';

class CashRegisterMenus extends StatelessWidget {
  const CashRegisterMenus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MenusFuture();
  }
}
